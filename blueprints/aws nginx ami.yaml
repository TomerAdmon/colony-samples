---
spec_version: 1
kind: blueprint

metadata:
  description: NGINX deployed from a predefined ami


clouds:
  - AWS: eu-west-1

applications:
  - nginx:
      instances: 1

configuration:
  healthcheck:
    wait_for_ports: ALL
    timeout: 180
